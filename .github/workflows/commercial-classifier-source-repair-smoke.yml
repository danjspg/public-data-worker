name: "[PUBLIC ONE-OFF] Commercial classifier source repair smoke"

on:
  push:
    branches:
      - oneoff/commercial-classifier-active
    paths:
      - ".github/commercial-classifier-source-repair-smoke-trigger"

permissions:
  contents: read

concurrency:
  group: public-commercial-classifier-source-repair-smoke
  cancel-in-progress: false

jobs:
  classify:
    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        shard: [0,1,2,3,4]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - name: Reclassify source-quality smoke row
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WORKER_DATABASE_URL: ${{ secrets.WORKER_DATABASE_URL }}
          COMMERCIAL_CLASSIFIER_MODE: classify
          COMMERCIAL_CLASSIFIER_SHARD: ${{ matrix.shard }}
          COMMERCIAL_CLASSIFIER_BATCH_SIZE: "1"
          COMMERCIAL_CLASSIFIER_FORCE_RECLASSIFY: "1"
          COMMERCIAL_CLASSIFIER_MANIFEST: data/commercial-classifier-source-repair-smoke.json
        run: npm run work:commercial-classifier-oneoff
